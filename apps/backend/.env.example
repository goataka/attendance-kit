# AWS Configuration
AWS_REGION=ap-northeast-1

# Environment
# NODE_ENVが設定されている場合、DynamoDBテーブル名は環境名から自動的に解決されます
# 例: NODE_ENV=dev の場合 → attendance-kit-dev-clock
NODE_ENV=dev

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production

# CORS Configuration
CORS_ORIGIN=*

# Port
PORT=3000
