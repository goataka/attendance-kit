{
  "name": "Attendance-Kit Development Container",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-20-bookworm",
  
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12"
    }
  },
  
  "postCreateCommand": "pip3 install uv && uv tool install specify-cli --from git+https://github.com/github/spec-kit.git && npm install && echo 'Setup complete. Node.js and spec-kit are ready to use.'",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "GitHub.copilot",
        "GitHub.copilot-chat",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "terminal.integrated.defaultProfile.linux": "bash",
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      }
    }
  },
  
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:${containerEnv:HOME}/.local/bin"
  },
  
  "forwardPorts": [3000, 5173, 4321],
  
  "postStartCommand": "echo '🚀 Attendance-Kit Development Container is ready! Use: npm run dev (all), npm run dev:frontend, npm run dev:backend, npm run dev:site'",
  
  "remoteUser": "node"
}
